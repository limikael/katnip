jwtSecret: "changeme"
adminUser: "admin"
adminPass: "admin"
apiPath: "admin"

collections:
  pages:
    policies:
    - roles: admin
    - operations: read
      roles: public
    fields:
      <Text id="content"/>
      <Image id="image"/>
