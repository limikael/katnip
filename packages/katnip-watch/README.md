This plugin hooks into the dev server and rebuilds your project whenever 
a file changes.

## Options

* watch - Method to use when watching for a file change. Choices are "fs", "chokidar" and "none".
